VITE_API_URL=http://localhost:4000

# Optional — only needed if the backend has GATEWAY_API_KEY set (see the
# root .env.example). When set here, every request from this frontend
# sends `Authorization: Bearer <this value>`. Must match the backend's
# GATEWAY_API_KEY exactly. Leave both unset to keep the gateway open.
VITE_API_KEY=
